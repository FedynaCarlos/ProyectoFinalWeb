<!DOCTYPE html>
<html lang="en">
<<%- include('./partials/head') %>
<head>
    <title>Crear Usuarios</title>
</head>
<body>
    <%- include('./partials/header') %>
    <main>
        <h2>Formulario de creación de Usuarios</h2>
        <!--Se realiza main con la finalidad de poder realizar un flexbox y poder manejar sus propiedades-->
        <form class="createUsers" action="/usuarios" method="POST" enctype="multipart/form-data">
        <!--<form action="" method="post" class="createUsers"> -->
           <div class="controlIngresoUsuarios">
               <label for="">Nombre del Usuario</label>
               <input class="input1" type="text" name="nombre" id="nombre" placeholder="Ej: Juan Perez">
               <% if (typeof errors != 'undefined' && typeof errors.nombre != 'undefined') { %>
                <span style="color: red"><%= errors.nombre.msg %></span> 
            <% } %>
           </div>
            <div class="controlIngresoUsuarios">
                <label for="">Nombre de usuario:</label>
                <input class="input1" type="nombre" name="nombre"  placeholder="Ej: 5.400">
                <% if (typeof errors != 'undefined' && typeof errors.precio != 'undefined') { %>
                    <span style="color: red"><%= errors.nombre.msg %></span> 
                <% } %>
            </div>
            
            <% if (typeof errors != 'undefined' && typeof errors.descripcion != 'undefined') { %>
                <span style="color: red"><%= errors.descripcion.msg %></span> 
            <% } %>
            <div class="controlIngresoUsuarios">
                <button type="submit" class="boton-guardarP">Guardar usuario</button>
            </div>
        </form>
</body>
</html>